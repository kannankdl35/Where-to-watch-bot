version: '3.8'

services:
  bot:
    build: .
    container_name: where-to-watch-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
